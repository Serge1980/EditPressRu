@{
    ViewBag.Title = "Ошибка 404";
    if (ViewBag.Categories==null)
    {
        List<SelectListItem> list = new List<SelectListItem>();
        SelectListItem listItem = new SelectListItem { Text = "Ошибка", Value = "Ошибка", Selected = true };
        list.Add(listItem);
        ViewBag.Categories = list;
       
    }

}

<div style="padding-top:45px;width:100%;height:100%; background:url(/images/404fon.png) top center no-repeat,url(/images/404fon_repeat.png);">
    <h1 class="error">Ошибка 404</h1>
    <h2 class="error">Что-то пошло не так.</h2>
    <h2 class="error">При обработке запроса произошел сбой, возможно в строке поиска ничего не было указано.</h2>
    <h2 class="error">Возможно в строке поиска был указан товар,которого нет у нас в каталоге.</h2>
    <h2 class="error">Пишите нам, мы заведем  у себя на сайте то, что Вам необходимо, и доставим Вам любой подарок с логотипом!</h2>
    <h2 class="error">Приносим извинения за неудобства.</h2>
    <p style="text-align:center;padding:30px 0px 30px 0px;"><a href="/" class="artikle_link" style="font-size:25px;">на главную</a></p>
    <p class="error">
        Если это реально наша недоделка или у Вас есть предложения по улучшению сайта - пишите нам
        <a href="#" class="backCall" style="padding:5px;border-radius:10px;background-image:none;">admin@editpress.ru</a>
    </p>
</div>

@*@if (Model != null && HttpContext.Current.IsDebuggingEnabled)
    {
        <div>
            <p>
                <b>Exception:</b> @Model.Exception.Message<br />
                <b>Controller:</b> @Model.ControllerName<br />
                <b>Action:</b> @Model.ActionName
            </p>
            <div style="overflow:scroll">
                <pre>
                    @Model.Exception.StackTrace
                </pre>
            </div>
        </div>
    }*@
